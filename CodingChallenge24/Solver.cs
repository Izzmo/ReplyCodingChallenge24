public class Solver
{
    public static void SolveR(Model model)
    {
        //for (var y = 0; y < model.GridColumns; y++)
        //{
        //    if (y >= model.Snakes.Count) break;

        //    var snake = model.Snakes[y];
        //    if (snake.SegmentLength >= model.GridWidth) continue;

        //    snake.Locations = new List<(int x, int y)>();

        //    for (var x = 0; x < snake.SegmentLength; x++)
        //    {
        //        snake.Locations.Add((x, y));
        //    }
        //}
    }
}